# RPZ Local Processor 事件處理進度

**更新時間**: 2026-09-04
**用途**: 對話壓縮前的進度快照。接手時先讀這份，再讀 `process.md`。

---

## 1. 一句話狀態

三個 patch 都已完成並通過 LAB 驗證，**尚未上正式機**：
1. **v4**（4096/SIGPIPE 修正，`e407d6e7…`）：兩輪審核 CONDITIONAL GO，
   全部 findings 已關閉。審核已授權關閉後進單機 canary。
2. **Phase 1B**（暫存檔保留策略，`aa97950e…`）：三輪審核，輪 3 判定
   **runtime GO**、P1B-01/08 關閉、e2e 驅動器 GO（限指定 LAB）。
   P1B-07 文件殘餘已於輪 3 後全部修正。**canary GO。**
3. **Phase 1C**（事件 log 改走 syslog，`a0ca535f…`）：第一輪審核
   日誌修正 **GO**、canary CONDITIONAL GO；P1C-01~03 已修正並重驗
   （迴歸 33、mock 反向測試全中止、gate 42）。**待短確認**。
   部署順序 v4 -> 1B -> 1C（1C check 驗 1B main；v4 由 SOP 交叉確認）。
安裝包 NO-GO 維持 HOLD（現為 v1.2.3）。CR-10 = Phase 2 P0。
LAB license 到期 2026/10/06（09-05 查詢）。細節見 `process.md` 第 19~29 節。

---

## 2. 根因（已證實）

三處 `ls -t <glob> | head -1` 在 `set -o pipefail` 下不安全。

| 位置 |
|---|
| `scripts/parse_rpz.sh:227` |
| `scripts/generate_datagroup.sh:65` |
| `scripts/generate_datagroup.sh:82` |

`ls` 對 pipe 的 stdio write buffer 是 4096 bytes。輸出超過就需要多次
`write()`。`head -1` 取到第一行即關閉 pipe，`ls` 後續 write 收到 SIGPIPE
以 141 結束。`pipefail` 讓管線回傳 141，`set -e` 終止腳本且**不留錯誤訊息**。

失敗訊息長這樣，看不出原因：

```
[INFO] 載入 2 個 Zones: rpztw phishtw
[ERROR] RPZ 解析失敗
=== Exit Code: 1 ===
```

判別方法：`載入 N 個 Zones` 之後直接 `[ERROR]`，中間少了
`[INFO] 使用 dnsxdump 檔案:`。

### 實測失敗率（BIG-IP 17.1.3.1，真實 `parse_rpz.sh`，每組 30 次）

| raw 檔數 | `ls` 輸出 | 失敗率 |
|---|---|---|
| 67 | 4087 B | 0% |
| 80 | 4880 B | 17% |
| 141 | 8601 B | 80% |
| 179 | 10919 B | 87% |
| 300 | 18300 B | 100% |

**安全上限是 `ls` 輸出低於 4096 bytes，不是固定的檔案數。** 換算值隨
zone 名稱與路徑長度變動：

| glob | 每行 | 安全上限 |
|---|---|---|
| `raw/dnsxdump_*.out` | 61 B | 67 檔 |
| `parsed/rpztw_*.txt` | 61 B | 67 檔 |
| `parsed/phishtw_*.txt` | 63 B | 65 檔 |
| 27 字元的 zone 名 | 83 B | 49 檔 |

---

## 3. 四台正式機狀況（2026-08-20 擷取）

| 設備 | /config | raw 檔數 | parsed 檔數 | 最後完整成功 | 停滯 |
|---|---|---|---|---|---|
| 設備A | 70% | 141 | 267 | 08-18 09:10 | 54.7 小時 |
| **設備B** | **84%（已告警）** | 179 | 285 | **08-13 16:10** | **超過 9 天** |
| 設備C | 80% | 169 | 282 | 08-14 13:55 | 超過 8 天 |
| 設備D | 60% | 125 | 201 | 08-19 15:05 | 24.9 小時 |

設備B 的 RPZ 黑名單從 08-13 停到現在。**這是唯一有時間壓力的事項。**

---

## 4. 更新頻率分析（重要，決定 KEEP 值）

| 項目 | 值 | 來源 |
|---|---|---|
| 平日頻率 | **18 次/日** | 08-19（星期三）擷圖實測，逐檔時間 |
| 週末頻率 | 約 3 次/日 | 四台窗口差反推 |
| 日間速率 | 2.0 次/小時 | 09:40 到 18:40 |
| 夜間 | 多數為 0，但 08-18 有 19:35 與 23:00 兩筆 | |
| 間隔 | 最短 5 分，最長 80 分，平均 32 分 | |
| burst | 四台同時在 15:15/15:20/15:25 連續三次 | wrapper log |
| 每次變更的磁碟量 | 16.79 MB（raw 11.5 + parsed 5.29） | |

平日/週末模型（18/3）驗算 設備D 的 8.92 天窗口：推算 129，實際 125，誤差 3.2%。

### 設計容量上限

| 上限 | 值 | 目前 |
|---|---|---|
| 原版設計的 SIGPIPE 上限 | **11.0 次/平日** | 18 次，超出 1.6 倍 |
| 8 天保留期的磁碟告警上限 | **25.6 次/平日** | 18 次，用掉 70% 餘裕 |

這解釋了「跑半年才壞」：頻率成長跨過 11 次/平日的隱性上限。

### 保留策略對頻率成長的反應

| 策略 | 磁碟用量 | 頻率翻倍時 |
|---|---|---|
| 保留 N 天（現行 8 天） | N × 頻率 × 16.8 MB | **磁碟翻倍** |
| 保留 N 個（KEEP） | N × 16.8 MB | **磁碟不變，可回溯時間減半** |

| KEEP | 磁碟 | 占 /config | 平日可回溯 |
|---|---|---|---|
| 7 | 118 MB | 3.7% | 3.5 小時 |
| 18 | 302 MB | 9.4% | 9.0 小時 |
| 24 | 403 MB | 12.6% | 12.0 小時 |
| 36 | 605 MB | 18.5% | 18.0 小時 |

---

## 5. 已完成的交付物

### 程式碼變更（tracked source）

| 檔案 | 原版 v1.2 md5 | 現行 md5 | 變更 |
|---|---|---|---|
| `scripts/utils.sh` | `3cab6cbca952f3780350e9882e5f7c11` | `b8294149dc978305e19bcd83fcb650e6` | +34 −2，新增 `find_newest_file()` |
| `scripts/parse_rpz.sh` | `bbe45c6f79b56922388d4af7aa6e7583` | `cefa71b6623632dd51c60a51cdf72196` | +5 −2 |
| `scripts/generate_datagroup.sh` | `35547d33ce109945d1ca17e8eb241e0a` | `9599755a54db53652c070cd70ae92652` | +47 −19，改為 resolve-then-publish |
| `scripts/main.sh`（1B+1C） | `0041c1d74e5b8514dea506608607b8c6` | `9d8538a68480a1a0489058be6b1d6622` | 1B: find 縮小、KEEP=24、trap EXIT 等；1C: 8 處事件 log 改 logger、註解勘誤修正（1B 凍結版 = `d1e1f688…`） |
| `scripts/extract_rpz.sh`（Phase 1C） | `62aeaf053b08f3411fe530f33555c414` | `fea7c2e29f5380ab22611f7b2cc97fbc` | 4 處事件 log 改 logger、補結尾 newline |
| `scripts/update_datagroup.sh`（Phase 1C） | `f8b038bc06df1c07050cd2922a91c5aa` | `67227cb39028dc2bf17b14ef9c871bc4` | 5 處事件 log 改 logger |

其餘一支未修改：`check_soa.sh` `19700e7c…`。

### 工具與文件

| 檔案 | md5 |
|---|---|
| `patches/rpz_patch_sigpipe_v4.sh`（**現行**，876 行，邏輯 268） | SHA-256 見下方 |
| `patches/rpz_patch_phase1b_v1.sh`（**現行**，591 行，邏輯 234） | SHA-256 見下方 |
| `patches/rpz_patch_phase1c_v1.sh`（**現行**，892 行，邏輯 245） | SHA-256 見下方 |
| `patches/build_patch_phase1c.sh` | 產生 1C patch，deterministic |
| `tests/lab/f5_patch_1c_test.sh` | 1C 迴歸測試，33 斷言（含 F4/F5 永久保護） |
| `tests/lab/f5_e2e_1c_controlled.sh` | 1C 受控 e2e 驅動（P1C-02 修正後，mock 反向測試通過） |
| `patches/build_patch_phase1b.sh` | 產生 1B patch，deterministic |
| `tests/lab/f5_patch_1b_test.sh` | 1B 迴歸測試，112 斷言（含 F12 前綴重疊） |
| `tests/lab/f5_e2e_1b_controlled.sh` | 1B 受控 e2e 驅動（fail-closed，20 斷言） |
| `patches/rpz_patch_sigpipe_v4.sh.sha256` | sidecar |
| `patches/build_patch_v4.sh` | 產生 v4；不依賴 git HEAD（V4-03）；deterministic |
| `tests/lab/f5_patch_v4_test.sh` | 獨立迴歸測試，78 斷言（含 R2-V4-02 案例） |
| `patches/archive/rpz_patch_sigpipe_v3.sh`（封存審核紀錄） | `3b50f5ec394aad24536d1ffc9851f4a8` |
| `patches/archive/rpz_patch_sigpipe_v3.sh.sha256` | sidecar |
| `patches/README.md` | 部署手冊（經銷商工程師用，STE100 改寫版） |
| `install.sh` | `f9fd0e2f106caee52aa36f597a3d2361` |
| `package.sh` | VERSION 1.2.3（產出包 HOLD） |
| `tests/check_source_consistency.sh` | payload 鏈模型（v4/1B凍結/1C），PASS=42 |
| `tests/lab/f5_hotfix_test.sh` | `4124f45adfd1869d2a221ba9567fe519` |
| `tests/lab/f5_manual_cleanup_test.sh` | `9e5e54874e119b49fcc0cfb8a1d83426`（匿名化後） |
| `tests/lab/f5_rate_probe.sh` | `bb6a2efd7af80262a1138f3748447882` |
| `tests/lab/f5_e2e_probe.sh` | `f1aacaf211daf8535a37d93ca5e235fa` |
| `tests/lab/f5_pipefail_probe.sh` | `796c518317360cd5b872b72a58e0d8ee`（匿名化後） |

SHA-256：

```
a0ca535f84f744cb50dfbdbe84e9dec7362d398968dd53bd33ee9d2de04610ec  patches/rpz_patch_phase1c_v1.sh (現行，審核輪1修正後，待短確認)
e407d6e7d0d12d1c6ca445d737208ab139437fd8504fe47d9b318754c1d37626  patches/rpz_patch_sigpipe_v4.sh (現行，審核輪2修正後)
aa97950e8a45541b8f48bcdfa4d20c495db6d9ef4989724d064dd3470058c785  patches/rpz_patch_phase1b_v1.sh (現行，審核輪2修正後)
9876a90aa04e3748a37e23a7af3af4720f45028877b6b8fb08c7a891dd60d7d0  patches/archive/rpz_patch_sigpipe_v3.sh (封存)
fe0aa51a8723205acb83722f3c8b290ae280b6dc414b6a1cfcb39616eaa92aae  dist/rpz_local_processor_v1.2.3_20260904_120903.tar.gz (最新，HOLD，P1B-05/06 NO-GO)
```

### 文件

| 檔案 | 行數 | 內容 |
|---|---|---|
| `process.md` | 2076 | 完整事件記錄。§16~18 v3、§19~21 v4、§22~25 1B、§26 soak、§28~29 1C |
| `docs/reviews/REVIEW_HANDOFF_PHASE1A.md` | 282 | 給第三輪審核的交接，回答九個問題 |
| `patches/README.md` | 395 | 部署手冊：三個 patch 的部署/驗證/還原（含 4.6 Patch 3 與跨 patch 還原順序）、常見狀況、背景記錄 |
| `RPZ_手動清檔作業說明_20260821.md` | 394 | 給經銷商工程師的手動清檔作業（v2） |
| `dist/DO_NOT_DEPLOY.md` | — | 舊 artifact 的部署狀態 |
| `tests/lab/README.md` | — | LAB 測試腳本的破壞性說明 |

### 審核紀錄

| 輪次 | 檔案 | 判定 |
|---|---|---|
| 1 | `docs/reviews/CODE_REVIEW_20260821.md` | NO-GO，CR-01~CR-16 |
| 2 | `docs/reviews/CODE_REVIEW_PHASE1A_ROUND2_20260821.md` | NO-GO，R2-01~R2-07 |
| 3 | `docs/reviews/CODE_REVIEW_PHASE1A_ROUND3_20260821.md` | 被 REV2 取代 |
| 3-REV2 | `docs/reviews/CODE_REVIEW_PHASE1A_ROUND3_REV2_20260821.md` | **CONDITIONAL GO**（v3），SHA-256 `1d7e9412…` 已核對 |
| v4-1 | `docs/reviews/CODE_REVIEW_V4_STE100_20260822.md` | **CONDITIONAL GO**（v4），V4-01~05 已修正並重驗，SHA-256 `9cd9d606…` 已核對 |
| v4-2 | `docs/reviews/CODE_REVIEW_V4_ROUND2_STE100_20260822.md` | **CONDITIONAL GO**（v4），R2-V4-01~03 已修正並重驗，SHA-256 `7577ca04…` 已核對 |
| 1B-1 | `docs/reviews/CODE_REVIEW_PHASE1B_STE100_20260823.md` | patch **CONDITIONAL GO** / e2e 驅動器與 v1.2.2 包 NO-GO / CR-10 升 P0。P1B-01~04 已修正並重驗，SHA-256 `7329d2c8…` 已核對 |
| 1B-2 | `docs/reviews/CODE_REVIEW_PHASE1B_ROUND2_STE100_20260823.md` | **CONDITIONAL GO**；02/03/04 關閉、01 降 Medium、新增 P1B-08。SHA-256 `f72f0298…` 已核對 |
| 1C-1 | `docs/reviews/CODE_REVIEW_PHASE1C_STE100_20260905.md` | 日誌修正 **GO** / canary CONDITIONAL GO / e2e 修正前 NO-GO。P1C-01~03 已修正並重驗，SHA-256 `e1f0d694…` 已核對 |
| 1B-3 | `docs/reviews/CODE_REVIEW_PHASE1B_ROUND3_STE100_20260823.md` | **runtime GO**；P1B-01/08 關閉、e2e 驅動器 GO（限 LAB）、canary 文件修正後 GO、package 維持 HOLD、CR-10 維持 P2 P0。SHA-256 `09bd82b4…` 已核對 |

第三輪的四項 P0 已全部完成（外部 SHA-256、cleanup false-success、SOP、重算 hash）。

---

## 6. LAB 狀態（`10.8.34.223`）

```
BIG-IP 17.1.3.1，bash 4.2.46，coreutils 8.22
admin 已設 ed25519 key 認證，uid=0
v4 三支 md5 = tracked source（b8294149 / cefa71b6 / 9599755a）
main.sh = Phase 1B 修正版（d1e1f688，審核輪2修正後），DataGroup revision 23
iCall rpz_processor_handler，interval 300，active
DataGroup rpztw / phishtw 正常更新（source = final/）
/config 5% 使用
temp fixture 已清除
DNS Express 由 Infoblox 10.8.38.225 餵 rpztw / phishtw
測試 VS vs_rpz_irule_test_udp/_tcp @ 10.8.38.199:53，掛 rpzdg_local_v2
```

`/var/tmp` 保留：v4 patch 與 sidecar、1B patch 與 sidecar（現行建置）、
兩個純原版備份（`rpz_patch_backup_20260822_232957` = v4 三支、
`rpz_patch1b_backup_20260823_015040` = main.sh）、`origsrc`、
八支 probe/test 腳本、dnsxdump 樣本。歷史暫存物已清除。

---

## 7. 待決定事項（5 項）

### 決定 1：過度設計的處理 — 已解決（2026-08-22）

使用者決定重新構築。已完成 v4（見第 5 節與 `process.md` 第 19 節）：
移除 cleanup 子指令、do_selftest、recovery staging，工具邏輯 987 -> 228 行。
在 LAB 恢復原版後完成 16 項測試，全數 PASS（含 2 項混合狀態功能實測）。

後續：兩輪 Codex 審核均 CONDITIONAL GO；V4-01～05（第 20 節）與
R2-V4-01～03（第 21 節）全部修正並重驗。第二輪審核明示：關閉三項
findings 後即進單機 canary，不必重跑 4096 curve 與 e2e。
新待決：直接 canary（用建置 `e407d6e7…`），或先送第三輪確認。

### 決定 2：部署方式

| 選項 | 內容 | 維護窗口 | rollback |
|---|---|---|---|
| A | 4096 與 Phase 1B 合併成一個 patch | 1 次 | 只能全退 |
| **B（我的建議）** | **兩個 patch，同一個窗口依序套用** | **1 次** | **可獨立退** |
| C | 兩個 patch，兩個窗口 | 2 次 | 可獨立退 |

使用者傾向同時修正 4096 與 Phase 1B。做法 B 在單一窗口達成，且 v4 與 Phase 1B 可獨立 rollback。

### 決定 3：Phase 1B 範圍 — 已解決（2026-08-23）

使用者選 **B1**：只改 `main.sh`（縮小 find、數量上限、`trap EXIT`）。
wrapper log rotate 不做（50 MB/年，不急）。

### 決定 4：KEEP 的值 — 已解決（2026-08-23）

使用者定 **KEEP=24**（每家族），磁碟用量估計 403 MB、占 `/config` 12.6%、
平日可回溯 12 小時。環境變數 `RPZ_KEEP_COUNT`，預設 24，範圍 1-99999。

已實作並通過三輪審核（輪 3 GO）。設計文件 `docs/PHASE1B_DESIGN_20260823.md`。

### 決定 5：`find` 範圍縮小要不要納入 4096 patch

已實作於 Phase 1B patch（縮小 find 範圍是其第 1 項變更）。

---

## 8. 已排除的選項

| 選項 | 為什麼排除 |
|---|---|
| 固定檔名改造 | 磁碟效果最好（17 MB）但歷史資料歸零。使用者需要保留歷史供除錯。筆數上限同時滿足兩個需求，改動又小得多（1 處 vs 8 處） |
| 只做 Phase 1B、不修 4096 | 已實測證明無效。見第 9 節 |

---

## 9. 關鍵實測結論（不要重做）

### Phase 1B 單獨無法解決 4096（原版腳本實測）

| 情境 | 結果 |
|---|---|
| KEEP=24 / 40 / 60 | 0/20 失敗（表面正常） |
| KEEP=100（設錯） | 5/20 失敗 |
| KEEP=150（設錯） | 14/20 失敗 |
| cleanup 沒跑、累積到 179 檔 | 16/20 失敗 |
| 27 字元 zone 名 + KEEP=60 | 2/20 失敗 |
| `rpztw` + KEEP=67 | 5/20 失敗（4148 B，超過 4096） |

四種復發途徑：KEEP 設錯、cleanup 沒跑（SIGKILL/OOM/timeout 時 `trap EXIT` 不執行）、
zone 名稱變長、安裝路徑變長。**而且復發是無聲的。**

### Gap 2：`find ""` 遞迴包含 `final/`

`scripts/main.sh:84` 是遞迴掃描，實測命中 14 個檔案，其中 4 個在 `final/`。

| 測試 | 條件 | 結果 |
|---|---|---|
| A | 原版成功執行，`final/` 為 30 天前 | `final/` 保留 3 個（step 4 先寫入，mtime 變新） |
| B | zone 已從 zonelist 移除 | `final/oldzone.txt` **被刪除** |
| **D** | **加 `trap EXIT`，步驟 3 失敗** | **`final/` 由 3 個變 0 個** |
| E | `find` 範圍縮小，同樣條件 | `final/` 保留 3 個，`raw/` 舊檔仍清除 |

**Phase 1B 必須先縮小 `find` 範圍，才能加 `trap EXIT`。順序不可顛倒。**

設備B（`final/` 08-13）與 設備C（08-14）已超過 8 天，
若先加 `trap EXIT` 會刪掉 DataGroup 來源。

### 目錄結構的修正

`.soa_cache/` 是 `install.sh:243` 建立的**空目錄，程式從未使用**。
SOA cache 實際在 `/config/snmp/.{zone}_soa_serial.last`
（`check_soa.sh:19` 的 `SOA_CACHE_DIR` 預設 `/config/snmp`）。

`rpz_wrapper.log` 在 `/config/snmp/`，比 `$OUTPUT_DIR` 高一層，
`find "$OUTPUT_DIR"` 與 patch 的 cleanup **都碰不到**。

`RPZ_Local_Processor/logs/` 是 `main.sh:52` 建立的空目錄，沒人寫入。

### cleanup 只在完整成功後執行（四台交叉驗證）

各台最舊 raw 檔 = 最後一次完整成功減 8 天，四台全部相符（差距 10~135 分鐘）。
這獨立證實 `-mtime +7` 保留 8 天、cleanup 只在成功時執行、
以 `final/rpzip.txt` mtime 推定最後成功時間正確。

### 缺陷 A 仍未修（v3 不動它）

`extract_rpz.sh:80` 用 `export DNSXDUMP_FILE` 傳給 `main.sh`，但
`main.sh:141` 是 `bash extract_rpz.sh`（獨立子行程），`export` 傳不回來。
`main.sh:88` 的條件永遠 false，`main.sh:89` 的 `rm -f` 從未執行。
四台 `grep -c "清理 dnsxdump 檔案完成"` 都回 0。

---

## 10. 待取得的資料

| 資料 | 用途 | 指令 |
|---|---|---|
| 四台的版本與腳本 md5 | **rollout 前置條件**（第三輪審核第 13 節） | `tmsh show sys version \| head -8` 與 `md5sum /config/snmp/RPZ_Local_Processor/scripts/*.sh` |
| 逐日檔案數統計 | 決定 KEEP 值 | `ls -l --time-style=long-iso /config/snmp/rpz_datagroups/raw/ \| awk 'NR>1{print $6}' \| sort \| uniq -c` |
| 四台的 SOA cache vs 實際 serial | 確認沒有 SOA 回退（CR-06） | `for z in rpztw phishtw; do echo "$z cache=$(cat /config/snmp/.${z}_soa_serial.last)"; done` 與 `dnsxdump \| grep SOA` |
| RPZ 更新頻率成長的原因 | 預估未來上限 | 問 Infoblox 端 |

前三項可在手動清檔前一併取得，成本低。

---

## 11. Phase 1B 清單（第 1~3 項已實作，見 `patches/rpz_patch_phase1b_v1.sh`）

| 順序 | 項目 | 位置 | 備註 |
|---|---|---|---|
| **1** | **縮小 `find` 範圍到 `raw/` 與 `parsed/`** | `main.sh:84` | **已完成** |
| 2 | 加筆數上限，與天數上限並用 | `main.sh:84` 附近 | 已完成（KEEP=24，範圍 1-99999） |
| 3 | 加 `trap EXIT` | `main.sh:253` | 已完成 |
| 4 | `rpz_wrapper.log` rotate | `rpz_wrapper.sh` | 決定 3 選 B2 才做 |
| 5 | 移除未使用的 `.soa_cache/` | `install.sh:243` | 對既有機器無效果 |

第 1 項的修法（測試 E 已驗證）：

```bash
find "$OUTPUT_DIR/raw"    -maxdepth 1 -type f -mtime +7 -delete 2>/dev/null || true
find "$OUTPUT_DIR/parsed" -maxdepth 1 -type f -mtime +7 -delete 2>/dev/null || true
```

---

## 12. Phase 2 待辦（`process.md` 第 12 節有完整清單）

| 編號 | 內容 |
|---|---|
| CR-06 | SOA 取得失敗被誤判為 `NO_UPDATE`（已在 LAB 重現） |
| CR-07 | SOA cache 在完整成功前就提交 |
| CR-08 | SOA serial 不支援回退（LAB 曾遇到） |
| CR-09 | 沒有全流程 lock |
| CR-10 | final 發布非原子、無資料完整性門檻 |
| CR-12 | `cleanup.sh` 的保留 DataGroup 選項不安全 |
| CR-13 | RPZ IP DataGroup 型別不一致 |
| CR-14 | iCall setup 的認證流程 |
| DOC-06 | 同步 `docs/SCRIPT_SPECIFICATIONS.md` 等規格文件 |

v1.2.1 新安裝包為 **HOLD**：installer 路徑處理、manifest symlink、
`INSTALL_GUIDE.txt` 過期都未處理。不阻擋既有機器套 patch。

---

## 13. git 狀態

**沒有 commit，沒有 push。**

已修改（未 commit）：

```
 M .gitignore
 M install.sh
 M package.sh
 M scripts/generate_datagroup.sh
 M scripts/parse_rpz.sh
 M scripts/utils.sh
```

未追蹤的新檔案：`process.md`、`patches/`、`tests/`、四份審核文件、
`docs/reviews/REVIEW_HANDOFF_PHASE1A.md`、`RPZ_手動清檔作業說明_20260821.md`、
`dist/DO_NOT_DEPLOY.md`、`STATUS_20260822.md`（本文件）、
`docs/` 下三份既有未追蹤文件。

commit message 草稿在對話中，需依決定 1 的結果調整範圍。

---

## 14. 下一步

0. 已完成真實來源 soak e2e（process.md 第 26 節）：Infoblox 每 6 分鐘
   新增一筆 x10，全數一個週期內拾取、dig 逐筆命中、prune 穩態封頂 24、
   零 ERROR/WARN，刪除方向同步正常。
1. **單機 canary（已獲審核 GO）**：同一窗口先 v4、後 1B，每台先 check；
   受控真實資料測試，**不播種合成檔**；之後觀察至少一個排程 tick，
   確認家族數有界且無 cleanup WARN。
3. 請經銷商工程師取得第 10 節的三項資料（rollout 前置）。
4. commit（需先提出 commit message 草稿供確認）。
5. Phase 2 P0 = CR-10（final 發布資料完整性門檻）。

**設備B 的黑名單已停超過 9 天，這是唯一有時間壓力的事項。**
